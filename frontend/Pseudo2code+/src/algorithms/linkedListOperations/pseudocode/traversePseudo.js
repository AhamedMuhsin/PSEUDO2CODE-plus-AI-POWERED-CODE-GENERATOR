export const traversePseudo = [
  "function traverse(head):",
  "  current = head",
  "  while current is not NULL:",
  "    print(current.data)",
  "    current = current.next",
  "  print('NULL')"
]
